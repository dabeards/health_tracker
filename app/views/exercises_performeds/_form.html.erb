<%= form_for(@exercises_performed) do |f| %>
  <% if @exercises_performed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exercises_performed.errors.count, "error") %> prohibited this exercises_performed from being saved:</h2>

      <ul>
      <% @exercises_performed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :calories_burned %><br>
    <%= f.text_field :calories_burned %>
  </div>
  <div class="field">
    <%= f.label :excercise_date %><br>
    <%= f.datetime_select :excercise_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
